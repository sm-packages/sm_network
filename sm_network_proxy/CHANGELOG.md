# Changelog

## 0.1.0

* Move Android and iOS system proxy discovery out of `sm_network`.
* Add Dio HTTP/1.1 and HTTP/2 proxy adapters.
* Add Swift Package Manager support for current Flutter projects.
* Support port 80 and IPv6 proxies and reject invalid proxy ports.
* Document direct fallback, proxy refresh, HTTP/2, and the Flutter example.
